1- imputacion valores nulos sdn_csv (imputacion a texto)
2- imputacion valores nulo-
3- match de aquellos sancionados tanto por UE como EEUU para ponerlos en el mismo registro
4- unir todo en mismo dataframe

-------------------------------
5 unir todas las variables en una sola variable de texto
6 procesar el texto con modelos ML de procesamiento de texto y obtener una columna con el peso de las palabras, lo que nos dará la nube de palabras más relevantes en la lista de sancionadosa
-------------------------------

5- Pasar las variables de texto a numéricas:
-Codificar los nombres de entidad con labelencoder y sacar latitud y longitud del país
-one hot encoding al tipo de sancionado

5- Aplicar modelo de clustering